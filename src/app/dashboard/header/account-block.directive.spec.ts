import { AccountBlockDirective } from './account-block.directive';

describe('AccountBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountBlockDirective();
    expect(directive).toBeTruthy();
  });
});
